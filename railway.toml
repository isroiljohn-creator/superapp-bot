[build]
builder = "NIXPACKS"

[services.web]
startCommand = "alembic upgrade head; python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"

[services.worker]
startCommand = "python -m bot.main"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
